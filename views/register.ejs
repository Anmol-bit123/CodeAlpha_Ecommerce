<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Register — ShopAlpha</title>
  <link rel="stylesheet" href="/style.css"/>
</head>
<body>
  <nav>
    <a href="/" class="brand">🛍 ShopAlpha</a>
    <div class="nav-links">
      <a href="/login">Log in</a>
    </div>
  </nav>

  <div class="container-sm">
    <% if (error.length) { %><div class="flash flash-error"><%= error %></div><% } %>
    <% if (success.length) { %><div class="flash flash-success"><%= success %></div><% } %>

    <div class="auth-card">
      <h1>Create account</h1>
      <p class="subtitle">Join ShopAlpha and start shopping today.</p>

      <form method="POST" action="/register">
        <div class="form-group">
          <label for="username">Username</label>
          <input type="text" id="username" name="username" placeholder="e.g. anmol123" required/>
        </div>
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" placeholder="you@example.com" required/>
        </div>
        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" placeholder="Min. 6 characters" required/>
        </div>
        <button type="submit" class="btn btn-primary btn-full">Create account</button>
      </form>

      <div class="auth-footer">
        Already have an account? <a href="/login">Log in</a>
      </div>
    </div>
  </div>
</body>
</html>
